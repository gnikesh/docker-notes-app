# docker-notes-app
A production-ready Docker/Docker Compose setup for a containerized web app featuring **Notes** and **Notebooks** services, each with its own dedicated database, behind a **reverse proxy** that routes requests to the correct container based on the request URI.
